machine:
  node:
    version: 6.10.1

dependencies:
  override:
    - echo -e "$NPM_USERNAME\n$NPM_PASSWORD\n$NPM_EMAIL" | npm login
    - npm i
    - npm run compile

deployment:
  release:
    tag: /[0-9]+\.[0-9]+\.[0-9]+/
    commands:
      - npm publish --access public
